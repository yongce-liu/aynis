1. 使用 tyro 管理命令行参数
2. 使用 loguru 管理输出
3. 将可复用的功能抽取为职责单一、边界清晰的独立模块，避免重复实现
4. 不保留已被当前实现完全替代的 legacy、兼容层或 fallback 路径；确认调用方和数据迁移完成并通过测试后，直接删除旧代码、旧入口及其专用测试。只有在明确的产品需求、运行环境或数据兼容约束仍然存在时才允许保留，并在代码中说明适用边界和退出条件
5. 写代码时要尽可能地考虑共享infra，避免后续代码冗余
6. 修改代码后使用 ruff 静态检查代码问题, 并格式化代码